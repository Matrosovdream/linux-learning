# Env / secrets
.env
*.local
*.env.local

# Practice lab — persistent data volumes & scratch work
practice/**/data/
practice/**/volumes/
practice/**/scratch/
# Your work inside the lab persists here but shouldn't be committed (keep the folder)
practice/server-data/*
!practice/server-data/.gitkeep

# Docker build leftovers
**/*.pid
**/*.log

# Editor / OS
.DS_Store
.idea/
*.swp
*~
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
